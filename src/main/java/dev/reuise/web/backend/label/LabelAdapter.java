package dev.reuise.web.backend.label;
import dev.reuise.web.backend.component.WebBackendComponentAdapter;
import dev.reuise.web.backend.label.Label.Options;
import dev.reuise.web.core.label.WebLabelAdapter;
/**
 * Label adapter implementation (Web backend).
 */
public class LabelAdapter implements WebLabelAdapter , WebBackendLabel , WebBackendComponentAdapter {
    /**
     * Creates a new label adapter.
     */
    public LabelAdapter(Options options) {
    }
}